# v0.2 physical evidence

This directory receives formal clean-tree hardware evidence. The physical
SHT30 link has passed a short sanity check, but the complete clean baseline
and repeated fault episodes have not yet been accepted. No performance numbers
are reported here until a full raw protocol log passes `hardware/evaluate.py`.

`results/scenarios.csv` is separate v0.1 synthetic evidence.
